{% extends 'base.html.twig' %}

{% block title %}ChampFormulaire index{% endblock %}

{% block body %}
    <h1>ChampFormulaire index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Diplome</th>
                <th>Langue</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for champ_formulaire in champ_formulaires %}
            <tr>
                <td>{{ champ_formulaire.id }}</td>
                <td>{{ champ_formulaire.diplome }}</td>
                <td>{{ champ_formulaire.langue }}</td>
                <td>
                    <a href="{{ path('app_champ_formulaire_show', {'id': champ_formulaire.id}) }}">show</a>
                    <a href="{{ path('app_champ_formulaire_edit', {'id': champ_formulaire.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_champ_formulaire_new') }}">Create new</a>
{% endblock %}
