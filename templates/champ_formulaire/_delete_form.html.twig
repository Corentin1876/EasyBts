<form method="post" action="{{ path('app_champ_formulaire_delete', {'id': champ_formulaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ champ_formulaire.id) }}">
    <button class="btn">Delete</button>
</form>
