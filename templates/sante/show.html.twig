{% extends 'base.html.twig' %}

{% block title %}Sante{% endblock %}

{% block body %}
    <h1>Sante</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sante.id }}</td>
            </tr>
            <tr>
                <th>Date_Dernier_Rappel_Vaccin_Antitetanique</th>
                <td>{{ sante.DateDernierRappelVaccinAntitetanique ? sante.DateDernierRappelVaccinAntitetanique|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>ObservationParticuliere</th>
                <td>{{ sante.ObservationParticuliere }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ sante.adresse }}</td>
            </tr>
            <tr>
                <th>Tel_MedecinTraitant</th>
                <td>{{ sante.TelMedecinTraitant }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_sante_index') }}">back to list</a>

    <a href="{{ path('app_sante_edit', {'id': sante.id}) }}">edit</a>

    {{ include('sante/_delete_form.html.twig') }}
{% endblock %}
