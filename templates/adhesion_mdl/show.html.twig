{% extends 'base.html.twig' %}

{% block title %}AdhesionMDL{% endblock %}

{% block body %}
    <h1>AdhesionMDL</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adhesion_m_d_l.id }}</td>
            </tr>
            <tr>
                <th>Montant_Adhesion</th>
                <td>{{ adhesion_m_d_l.MontantAdhesion }}</td>
            </tr>
            <tr>
                <th>Mode_Reglement</th>
                <td>{{ adhesion_m_d_l.ModeReglement }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_adhesion_m_d_l_index') }}">back to list</a>

    <a href="{{ path('app_adhesion_m_d_l_edit', {'id': adhesion_m_d_l.id}) }}">edit</a>

    {{ include('adhesion_mdl/_delete_form.html.twig') }}
{% endblock %}
