{% extends 'base.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block body %}
    <h1>Inscription</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ inscription.id }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ inscription.statut }}</td>
            </tr>
            <tr>
                <th>Date_soumission</th>
                <td>{{ inscription.dateSoumission }}</td>
            </tr>
            <tr>
                <th>Annee_scolairen</th>
                <td>{{ inscription.anneeScolairen }}</td>
            </tr>
            <tr>
                <th>Annee_scolaire</th>
                <td>{{ inscription.anneeScolaire }}</td>
            </tr>
            <tr>
                <th>Specialisation</th>
                <td>{{ inscription.specialisation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_inscription_index') }}">back to list</a>

    <a href="{{ path('app_inscription_edit', {'id': inscription.id}) }}">edit</a>

    {{ include('inscription/_delete_form.html.twig') }}
{% endblock %}
