<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
        }
        .container {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            background-color: #000091;
            color: white;
            padding: 20px;
            text-align: center;
        }
        .content {
            background-color: #f5f5f5;
            padding: 20px;
            margin-top: 20px;
        }
        .info-row {
            margin-bottom: 15px;
        }
        .label {
            font-weight: bold;
            color: #000091;
        }
        .message-box {
            background-color: white;
            padding: 15px;
            margin-top: 15px;
            border-left: 4px solid #000091;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Nouveau message de contact</h1>
        </div>

        <div class="content">
            <div class="info-row">
                <span class="label">De :</span>
                {{ contact.civilite == 'mr' ? 'M.' : 'Mme' }} {{ contact.prenom }} {{ contact.nom }}
            </div>

            <div class="info-row">
                <span class="label">Email :</span>
                <a href="mailto:{{ contact.email }}">{{ contact.email }}</a>
            </div>

            {% if contact.telephone %}
            <div class="info-row">
                <span class="label">Téléphone :</span>
                {{ contact.telephone }}
            </div>
            {% endif %}

            <div class="info-row">
                <span class="label">Sujet :</span>
                {% if contact.sujet == 'inscription' %}Question sur l'inscription
                {% elseif contact.sujet == 'documents' %}Documents requis
                {% elseif contact.sujet == 'specialisations' %}Spécialisations disponibles
                {% elseif contact.sujet == 'calendrier' %}Calendrier et dates importantes
                {% elseif contact.sujet == 'technique' %}Problème technique
                {% else %}Autre demande
                {% endif %}
            </div>

            <div class="info-row">
                <span class="label">Date :</span>
                {{ contact.createdAt|date('d/m/Y à H:i', 'Europe/Paris') }}
            </div>

            <div class="message-box">
                <strong>Message :</strong><br><br>
                {{ contact.message|nl2br }}
            </div>
        </div>
    </div>
</body>
</html>