<form method="post" action="{{ path('app_contact_urgence_delete', {'id': contact_urgence.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contact_urgence.id) }}">
    <button class="btn">Delete</button>
</form>
