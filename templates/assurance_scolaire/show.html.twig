{% extends 'base.html.twig' %}

{% block title %}AssuranceScolaire{% endblock %}

{% block body %}
    <h1>AssuranceScolaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ assurance_scolaire.id }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ assurance_scolaire.adresse }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ assurance_scolaire.numero }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ assurance_scolaire.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_assurance_scolaire_index') }}">back to list</a>

    <a href="{{ path('app_assurance_scolaire_edit', {'id': assurance_scolaire.id}) }}">edit</a>

    {{ include('assurance_scolaire/_delete_form.html.twig') }}
{% endblock %}
