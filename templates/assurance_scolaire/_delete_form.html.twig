<form method="post" action="{{ path('app_assurance_scolaire_delete', {'id': assurance_scolaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ assurance_scolaire.id) }}">
    <button class="btn">Delete</button>
</form>
